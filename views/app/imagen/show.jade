extends ../../layout.jade
block contenido
	.image-container
		h1= imagen.title
		img(src="/public/imgs/#{imagen._id}.#{imagen.extension}", alt="imagen")
		if (typeof imagen.creator !="undefined")
			p Por #{imagen.creator.email}
			